<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>打怪兽小游戏</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background: #f0f0f0;
            margin: 0;
            padding: 0;
            overflow: hidden;
            position: relative;
        }
        .game-container {
            position: relative;
            width: 100vw;
            height: 100vh;
            background: #87ceeb;
            overflow: hidden;
        }
        .monster {
            position: absolute;
            width: 100px;
            height: 100px;
            background: url('https://example.com/monster.png') no-repeat center center;
            background-size: contain;
            cursor: pointer;
        }
        .score-board {
            position: absolute;
            top: 20px;
            left: 20px;
            font-size: 24px;
            color: #333;
        }
        .timer-board {
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 24px;
            color: #333;
        }
    </style>
</head>
<body>
    <div class="game-container">
        <div class="score-board">分数: <span id="score">0</span></div>
        <div class="timer-board">时间: <span i
